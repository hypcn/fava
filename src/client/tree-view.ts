
export class TreeView {

  // Have the location be the root
  // Load a node's children on opening the node
  // Discard the node's children on closing the node
  //  - Forces re-fetch on open, providing built-in refresh

  constructor() {}

}
